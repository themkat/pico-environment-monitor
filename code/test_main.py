# a very simple test main that can easily be run on device
# (rename to code.py for automatic runs)
import unittest

# primitive test discovery
from envmon_tests import *
from report_tests import *

unittest.main()
